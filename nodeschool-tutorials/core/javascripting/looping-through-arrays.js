// Define an array of strings
var pets = ['cat', 'dog', 'rat'];

// loop the array to concatenate
for (var i = 0; i < pets.length; i++) {
	pets[i] += 's';
}

// Print the concatenated values of array
console.log(pets);
